<!-- Mobile Drawer Backdrop -->
<div class="nav-backdrop" id="navBackdrop"></div>

<nav class="navbar sticky-top">
  <div class="container-fluid px-2 px-lg-4 d-flex align-items-center justify-content-between gap-3">

    <!-- Left Div: Brand -->
    <div class="d-flex align-items-center">
      <a class="navbar-brand d-flex align-items-center m-0" href="/listings">
        <i class="fa-solid fa-house"></i>
        <span class="brand-text ms-2">Rentlyst</span>
      </a>
    </div>

    <!-- Middle Div: Search Bar (Center, visible md+) -->
    <div class="d-none d-md-flex align-items-center mx-auto" style="flex: 0 1 350px; min-width: 70px;">
      <form class="search-bar w-100 m-0" role="search" method="GET" action="/listings/search">
        <input class="form-control search-input" type="search" placeholder="Search listings..." name="q" />
        <button class="btn search-btn" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>
    </div>

    <!-- Right Section: All Links + Auth + User Menu -->
    <div class="d-flex align-items-center gap-1 gap-md-3 ">

      <!-- Primary Links (Moved from left) -->
      <a class="nav-link nav-pill" href="/listings"><i class="fa-solid fa-compass fa-lg"></i><span
          class="d-none d-lg-inline ms-1">Explore</span></a>
      <a class="nav-link nav-pill d-none d-md-flex align-items-center" href="/listings/new"><i
          class="fa-solid fa-plus fa-lg"></i><span class="d-none d-lg-inline ms-1">Add Listing</span></a>

      <!-- Auth Links -->
      <% if(currUser) { %>
        <a class="nav-link nav-pill" href="/messages"><i class="fa-regular fa-comment-dots fa-lg"></i><span
            class="d-none d-lg-inline ms-1">Messages</span></a>
        <a class="nav-link nav-pill" href="/agent"><i class="fa-solid fa-robot fa-lg"></i><span
            class="d-none d-lg-inline ms-1">Ask Rentlyst</span></a>
        <% } %>

          <% if(!currUser) { %>
            <a class="nav-link nav-pill d-none d-md-flex" href="/signup"><i class="fa-solid fa-user-plus"></i><span
                class="d-none d-lg-inline ms-1">Sign up</span></a>
            <a class="nav-link nav-pill d-none d-md-flex" href="/login"><i
                class="fa-solid fa-right-to-bracket"></i><span class="d-none d-lg-inline ms-1">Log in</span></a>
            <% } %>

              <% if(currUser) { %>
                <!-- Desktop User Dropdown -->
                <div class="dropdown ms-1 d-none d-md-block">
                  <div class="user-menu" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="fa-solid fa-bars"></i>
                    <div class="host-avatar"
                      style="width:32px;height:32px;font-size:0.9rem;overflow:hidden;border-radius:50%;">
                      <% if(locals.currProfile && currProfile.profileImg && currProfile.profileImg.url &&
                        !currProfile.profileImg.url.includes('pexels.com')) { %>
                        <img src="<%= currProfile.profileImg.url %>" alt="Profile"
                          style="width:100%;height:100%;object-fit:cover;">
                        <% } else { %>
                          <%= currUser.username ? currUser.username.charAt(0).toUpperCase() : 'U' %>
                            <% } %>
                    </div>
                  </div>
                  <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0 mt-2"
                    aria-labelledby="dropdownMenuButton1">
                    <li><a class="dropdown-item fw-semibold" href="/profile">My Profile</a></li>
                    <li>
                      <hr class="dropdown-divider">
                    </li>
                    <li><a class="dropdown-item" href="/logout">Log out</a></li>
                  </ul>
                </div>

                <!-- Mobile Avatar Hamburger -->
                <div class="mobile-avatar-pill d-md-none ms-1" id="mobileMenuBtn">
                  <i class="fa-solid fa-bars"></i>
                </div>
                <% } else { %>
                  <!-- Mobile Login Hamburger -->
                  <button class="mobile-menu-btn d-md-none ms-1" id="mobileMenuBtn" aria-label="Open menu">
                    <i class="fa-solid fa-bars"></i>
                  </button>
                  <% } %>
    </div>

  </div>
</nav>

<!-- Mobile Drawer -->
<div class="mobile-drawer" id="mobileDrawer">
  <div class="drawer-header">
    <span class="fw-semibold" style="font-size:1.05rem;">Menu</span>
    <button class="drawer-close" id="drawerClose" aria-label="Close">
      <i class="fa-solid fa-xmark"></i>
    </button>
  </div>

  <!-- Same pill search as desktop -->
  <div class="drawer-search">
    <form class="search-bar w-100" role="search" method="GET" action="/listings/search">
      <input class="form-control search-input" type="search" placeholder="Search listings..." name="q" />
      <button class="btn search-btn" type="submit">
        <i class="fa-solid fa-magnifying-glass"></i>
      </button>
    </form>
  </div>

  <div class="drawer-nav">
    <a class="drawer-link" href="/listings"><i class="fa-solid fa-compass"></i><span>Explore</span></a>
    <a class="drawer-link" href="/listings/new"><i class="fa-solid fa-plus"></i><span>Add New Listing</span></a>
    <% if(currUser) { %>
      <a class="drawer-link" href="/messages"><i class="fa-regular fa-comment"></i><span>Messages</span></a>
      <a class="drawer-link" href="/agent"><i class="fa-solid fa-robot"></i><span>Ask Rentlyst</span></a>
      <div class="drawer-divider"></div>
      <a class="drawer-link" href="/profile"><i class="fa-solid fa-user"></i><span>My Profile</span></a>
      <a class="drawer-link drawer-link-danger" href="/logout"><i class="fa-solid fa-right-from-bracket"></i><span>Log
          out</span></a>
      <% } else { %>
        <div class="drawer-divider"></div>
        <a class="drawer-link" href="/signup"><i class="fa-solid fa-user-plus"></i><span>Sign up</span></a>
        <a class="drawer-link drawer-link-primary" href="/login"><i class="fa-solid fa-right-to-bracket"></i><span>Log
            in</span></a>
        <% } %>
  </div>
</div>

<script>
  (function () {
    const btn = document.getElementById('mobileMenuBtn');
    const drawer = document.getElementById('mobileDrawer');
    const backdrop = document.getElementById('navBackdrop');
    const closeBtn = document.getElementById('drawerClose');
    function openDrawer() { drawer.classList.add('open'); backdrop.classList.add('visible'); document.body.style.overflow = 'hidden'; }
    function closeDrawer() { drawer.classList.remove('open'); backdrop.classList.remove('visible'); document.body.style.overflow = ''; }
    if (btn) btn.addEventListener('click', openDrawer);
    if (closeBtn) closeBtn.addEventListener('click', closeDrawer);
    if (backdrop) backdrop.addEventListener('click', closeDrawer);

    // Apply active class to the current page link so it stays active
    const currentPath = window.location.pathname;
    const navLinks = document.querySelectorAll('.nav-pill, .drawer-link');
    navLinks.forEach(link => {
      const href = link.getAttribute('href');
      if (href) {
        if (currentPath === href) {
          link.classList.add('active');
        } else if (href === '/listings' && currentPath.startsWith('/listings') && !currentPath.includes('/new') && !currentPath.includes('/search')) {
          link.classList.add('active');
        }
      }
    });
  })();
</script>